name = "uptime-monitor"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Route + flags — declared here to match remote exactly so wrangler deploy
# does not override Dashboard settings on every push.
workers_dev = true
preview_urls = true

routes = [
  { pattern = "uptime.ews.ge", zone_name = "ews.ge", custom_domain = true }
]

[triggers]
# CF cron minimum granularity is 1 minute.
# Sub-minute intervals (e.g. 30s) are stored per-monitor but actual
# check frequency is capped at ~60s by this cron. See README for details.
crons = ["* * * * *"]

[[kv_namespaces]]
binding = "KV"
id = "96b90660c75c4ecb992fc4aa0b830365"

# Secrets — set via `wrangler secret put`, NOT stored here:
#   wrangler secret put ADMIN_PASSWORD
#   wrangler secret put SECRET_KEY
